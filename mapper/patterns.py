PERSONAL_DATA_KEYWORDS = [
    "email", "phone", "name", "address", "dob",
    "birth", "ssn", "password", "ip_address",
    "location", "gender", "user_id", "national_id"
]

SINK_KEYWORDS = [
    "requests.post", "requests.get", "cursor.execute",
    "send_mail", "log", "print", "insert", "save"
]
